spring:
  data:
    redis:
      host: redis

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/

management:
  tracing:
    enabled: false
  zipkin:
    tracing:
      endpoint: http://zipkin:9411/api/v2/spans
